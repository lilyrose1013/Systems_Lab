<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{'/_styles/style.css' | prefixedUrl}}">
</head>


<body>
    {% include "nav.njk" %}


    <main>
        {{ content | safe }}
 </main>


    {% include "footer.njk" %}


     {% if collections.post and collections.post.length > 0 %}
        <h2>Latest Posts</h2>
        <ul>
            {% for post in collections.post %}
            <li><a href="{{ post.url | prefixedUrl }}">{{ post.data.title }}</a> – {{ post.data.date | readableDate }}</li>
            {% endfor %}
        </ul>
    {% else %}
        <p>No posts found in collections.post</p>
        {% if collections.post %}
            <p>Collections.post exists but is empty (length: {{ collections.post.length }})</p>
        {% else %}
            <p>Collections.post does not exist</p>
        {% endif %}
    {% endif %}
</body>
</html>
