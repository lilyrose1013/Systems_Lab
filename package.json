{
  "name": "systems_lab",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
 "scripts": {
    "clean": "powershell -Command \"if (Test-Path docs) { Remove-Item -Recurse -Force docs }; if (Test-Path _site) { Remove-Item -Recurse -Force _site }\"",
    "build": "powershell -Command \"$env:ELEVENTY_ENV='production'; npx @11ty/eleventy\"",
    "start": "npx @11ty/eleventy --serve",
    "serve": "npx @11ty/eleventy --serve"
  },

  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
